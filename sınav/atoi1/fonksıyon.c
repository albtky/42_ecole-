#include <unistd.h>

int main(int ac, char *av[])
{
    if (ac == 2)
    {
        int i = 0;
        while (av[1])
        {
            if ((av[1][i] >= 'A' && av[1][i] <= 'M') || (av[1][i] >= 'a' && av[1][i] <= 'm'))
            {
                av[][]
            }
        }
    }
}